import React from "react";
import { Empty } from "../definitions";

const Complaint: React.VFC<Empty> = (
    _props: Empty,
    _context?: unknown
): React.ReactElement => {
    return <></>;
};

export default Complaint;
